# Database settings
DB_PASSWORD=your_secure_password_here
POSTGRES_PASSWORD=${DB_PASSWORD}
POSTGRES_USER=tournament_user
POSTGRES_DB=tournament
DATABASE_URL=postgres://tournament_user:${DB_PASSWORD}@db:5432/tournament

# Django settings
DJANGO_SECRET_KEY=generate_a_secure_random_key_here
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1

# Application settings
PYTHONPATH=/app

# Deployment settings
DJANGO_SETTINGS_MODULE=project.settings

# Instructions:
# 1. Copy this file to .env
# 2. Replace placeholder values with your actual secrets
# 3. Never commit your actual .env file to version control 